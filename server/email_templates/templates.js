const sendLinkTemplate = {
  template:
    "Hello ${userName} Please use this link for starting your session with the chatbot.",
  subject: "Audio Visual Chatbot Link",
};

module.exports = {
  sendLinkTemplate,
};
